import { SafeAreaView, StyleSheet, Text, View, Image, Button, Platform, StatusBar } from 'react-native';
import Homepage from './app/screens/Homepage';

export default function App() {
  

  return (
    <Homepage/>
  );
}